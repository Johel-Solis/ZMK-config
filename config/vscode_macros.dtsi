/ {
  behaviors {
    vs_select_all: vs_sel_all { compatible = "zmk,behavior-macro"; label = "VS_SELECTALL"; #binding-cells = <0>; bindings = <&kp LC(A)>; };
    vs_copy: vs_copy { compatible = "zmk,behavior-macro"; label = "VS_COPY"; #binding-cells = <0>; bindings = <&kp LC(C)>; };
    vs_fnext: vs_fnext { compatible = "zmk,behavior-macro"; label = "VS_FNEXT"; #binding-cells = <0>; bindings = <&kp LC(D)>; };
    vs_find: vs_find { compatible = "zmk,behavior-macro"; label = "VS_FIND"; #binding-cells = <0>; bindings = <&kp LC(F)>; };
    vs_replace: vs_replace { compatible = "zmk,behavior-macro"; label = "VS_REPLACE"; #binding-cells = <0>; bindings = <&kp LC(H)>; };
    vs_slctline: vs_slctline { compatible = "zmk,behavior-macro"; label = "VS_SLCTLINE"; #binding-cells = <0>; bindings = <&kp LC(L)>; };
    vs_new: vs_new { compatible = "zmk,behavior-macro"; label = "VS_NEW"; #binding-cells = <0>; bindings = <&kp LC(N)>; };
    vs_open: vs_open { compatible = "zmk,behavior-macro"; label = "VS_OPEN"; #binding-cells = <0>; bindings = <&kp LC(O)>; };
    vs_qopen: vs_qopen { compatible = "zmk,behavior-macro"; label = "VS_QOPEN"; #binding-cells = <0>; bindings = <&kp LC(P)>; };
    vs_save: vs_save { compatible = "zmk,behavior-macro"; label = "VS_SAVE"; #binding-cells = <0>; bindings = <&kp LC(S)>; };
    vs_paste: vs_paste { compatible = "zmk,behavior-macro"; label = "VS_PASTE"; #binding-cells = <0>; bindings = <&kp LC(V)>; };
    vs_close: vs_close { compatible = "zmk,behavior-macro"; label = "VS_CLOSE"; #binding-cells = <0>; bindings = <&kp LC(W)>; };
    vs_cut:   vs_cut   { compatible = "zmk,behavior-macro"; label = "VS_CUT";   #binding-cells = <0>; bindings = <&kp LC(X)>; };
    vs_redo:  vs_redo  { compatible = "zmk,behavior-macro"; label = "VS_REDO";  #binding-cells = <0>; bindings = <&kp LC(Y)>; };
    vs_undo:  vs_undo  { compatible = "zmk,behavior-macro"; label = "VS_UNDO";  #binding-cells = <0>; bindings = <&kp LC(Z)>; };
    vs_tgcomment: vs_tgcomment { compatible="zmk,behavior-macro"; label="VS_TGCOMMENT"; #binding-cells=<0>; bindings=<&kp LC(SLASH)>; };
    vs_sallwd:    vs_sallwd    { compatible="zmk,behavior-macro"; label="VS_SALLWD";    #binding-cells=<0>; bindings=<&kp LC(F2)>; };
    vs_split:     vs_split     { compatible="zmk,behavior-macro"; label="VS_SPLIT";     #binding-cells=<0>; bindings=<&kp LC(BKSLH)>; };
    vs_indent:    vs_indent    { compatible="zmk,behavior-macro"; label="VS_INDENT";    #binding-cells=<0>; bindings=<&kp LC(RBKT)>; };
    vs_outdent:   vs_outdent   { compatible="zmk,behavior-macro"; label="VS_OUTDENT";   #binding-cells=<0>; bindings=<&kp LC(LBKT)>; };
    vs_qfix:      vs_qfix      { compatible="zmk,behavior-macro"; label="VS_QFIX";      #binding-cells=<0>; bindings=<&kp LC(DOT)>; };
    vs_zoom_in:   vs_zoom_in   { compatible="zmk,behavior-macro"; label="VS_ZOOM_IN";   #binding-cells=<0>; bindings=<&kp LC(EQUAL)>; };
    vs_zoom_out:  vs_zoom_out  { compatible="zmk,behavior-macro"; label="VS_ZOOM_OUT";  #binding-cells=<0>; bindings=<&kp LC(MINUS)>; };
    vs_dline:     vs_dline     { compatible="zmk,behavior-macro"; label="VS_DLINE";     #binding-cells=<0>; bindings=<&kp RCS(K)>; };
    vs_symbols:   vs_symbols   { compatible="zmk,behavior-macro"; label="VS_SYMBOLS";   #binding-cells=<0>; bindings=<&kp RCS(O)>; };
    vs_sproblems: vs_sproblems { compatible="zmk,behavior-macro"; label="VS_SPROBLEMS"; #binding-cells=<0>; bindings=<&kp RCS(M)>; };
    vs_toggle_block_comment: vs_tblock { compatible="zmk,behavior-macro"; label="VS_TOGGLE_BLOCK_COMMENT"; #binding-cells=<0>; bindings=<&kp LSA(A)>; };
    vs_mlinedown: vs_mlinedown { compatible="zmk,behavior-macro"; label="VS_MLINEDOWN"; #binding-cells=<0>; bindings=<&kp LA(DOWN)>; };
    vs_mlineup:   vs_mlineup   { compatible="zmk,behavior-macro"; label="VS_MLINEUP";   #binding-cells=<0>; bindings=<&kp LA(UP)>; };
    vs_t_findword: vs_t_findword { compatible="zmk,behavior-macro"; label="VS_T_FINDWORD"; #binding-cells=<0>; bindings=<&kp LA(W)>; };
    vs_pdefinition: vs_pdefinition { compatible="zmk,behavior-macro"; label="VS_PDEFINITION"; #binding-cells=<0>; bindings=<&kp LA(F12)>; };
    vs_references: vs_references   { compatible="zmk,behavior-macro"; label="VS_REFERENCES";  #binding-cells=<0>; bindings=<&kp LS(F12)>; };
  };
};
